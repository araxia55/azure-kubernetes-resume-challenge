apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    run: web-service
spec:
  selector:
    app: ecom-webapp
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: LoadBalancer